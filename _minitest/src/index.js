// Minimal demo entry to test core build pipeline
document.addEventListener('DOMContentLoaded', () => {
  const el = document.createElement('div');
  el.id = 'minitest-root';
  el.textContent = 'MINIBUILD OK';
  document.body.appendChild(el);
});
