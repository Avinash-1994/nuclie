{
    "name": "urja-cloud-api",
    "version": "1.0.0",
    "description": "Production backend for Urja Self-Learning AI",
    "main": "server.ts",
    "scripts": {
        "dev": "bun run server.ts",
        "build": "bun build server.ts --outdir=dist --target=node",
        "start": "NODE_ENV=production bun run dist/server.js",
        "deploy": "vercel deploy --prod"
    },
    "dependencies": {
        "hono": "^4.0.0",
        "@supabase/supabase-js": "^2.39.0",
        "hono-rate-limiter": "^0.3.0"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "typescript": "^5.3.0"
    },
    "engines": {
        "node": ">=20.0.0"
    }
}