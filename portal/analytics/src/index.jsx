import React from 'react';
import { createRoot } from 'react-dom/client';
import Analytics from './Analytics.jsx';

createRoot(document.getElementById('root')).render(
  <React.StrictMode><Analytics /></React.StrictMode>
);
